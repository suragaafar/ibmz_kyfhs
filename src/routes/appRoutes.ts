/**
 * Browser path constants — keep in sync with `<Route>` definitions in `app/App.tsx`.
 */
export const AppRoutes = {
  home: '/',
  login: '/login',
  register: '/register',
  forgotPassword: '/forgot-password',
  resetPassword: '/reset-password',
} as const;
